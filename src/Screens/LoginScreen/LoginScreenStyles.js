import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "relative",
    flex: 1,
    width: "100%",
  },
  bgImage: {
    paddingTop: 323,
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});

export const { container, bgImage } = styles;
