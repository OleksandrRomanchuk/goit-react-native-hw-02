import MainView from "../../../module/MainView/MainView";

const MapScreen = ({ route, navigation }) => {
  return <MainView route={route} navigation={navigation}></MainView>;
};

export default MapScreen;
